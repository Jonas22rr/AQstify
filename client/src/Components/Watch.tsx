import { FunctionComponent as FC } from "react";
import Header from "./Header";

const Watch: FC = () => {
    return (
        <div>
            <Header id="watch" />
        </div>
    );
};

export default Watch;
